# Frontend environment variables
# Required: Base URL for backend REST API (no trailing slash), e.g. http://localhost:3001
REACT_APP_API_BASE_URL=http://localhost:3001

# Optional: Socket.IO server URL; if not set, API base will be used
REACT_APP_SOCKET_URL=http://localhost:3001
